{
  "version": 2,
  "buildCommand": "npm run build --prefix backend",
  "outputDirectory": "backend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    }
  ]
}
